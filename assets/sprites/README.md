Unit sprite sheets go here (e.g. scout-bat.png).
Load them in src/scenes/BootScene.js preload(), and set the frame layout in
data/units.js. A unit that has a real texture skips the placeholder art
automatically.

Remember to record every asset in ASSETS.md with its author,
licence and URL before committing it.
